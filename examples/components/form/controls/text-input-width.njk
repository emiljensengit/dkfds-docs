{% from "components/form/controls/text-input-width.njk" import textinputwidth as textinputwidth %}
{% from "components/form/controls/text-input-width.njk" import textinputchar as textinputchar %}

<div class="container">
  <h4>Input bredde med rem</h4>
  {% for item in textinputwidth_example.items %}
    {{textinputwidth(item.id, item.label, item.optionallabel, item.class, item.name, item.classLabel)}}
  {% endfor %}
</div>

<div class="container">
  <h4 class="mt-7">Input bredde med tegn</h4>
  {% for item in textinputchar_example.items %}
    {{textinputchar(item.id, item.label, item.optionallabel, item.class, item.name, item.classLabel)}}
  {% endfor %}
</div>

