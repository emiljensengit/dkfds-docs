{% from "components/side-navigation/sidenav.njk" import nav_list as nav_list %}

<div class="container mt-6 mb-6">
      <div class="row">
            <div class="col-12 col-sm-12 col-md-4 col-lg-4 sidebar-col">
              <h5>Normal sidenavigation</h5>
              {% for nav in sidenavsNormal %}
              <h5 class="mt-6">{{ nav.title }}</h5>
              <div class="container">
                <div class="row">
                  <div class="col-12 sidebar-col">
                    <nav>
                      <ul class="sidenav-list">
                        {{ nav_list(nav.links, 'sidenav-sub_list') }}
                      </ul>
                    </nav>
                  </div>
                </div>
              </div>
              {% endfor %}
            </div>
             {# <div class="col-12 col-sm-12 col-md-4 col-lg-4 sidebar-col">
                  <h5 class="mt-6 mt-md-0">Sidenavigation med ikon</h5>
                  {% for nav in sidenavsIcon %}
                  <h5 class="h5 mt-6">{{ nav.title }}</h5>
                      <div class="container">
                        <div class="row">
                          <div class="col-12 sidebar-col">
                            <nav>
                              <ul class="sidenav-list">


                                    {{ nav_list(nav.links) }}

                      </ul>
                    </nav>
                  </div>
                </div>
              </div>
              {% endfor %}
            </div> #}
            <div class="col-12 col-sm-12 col-md-4 col-lg-4 sidebar-col">
              <h5 class="mt-6 mt-md-0">Sidenavigation med information</h5>
              {% for nav in sidenavsInformation %}
              <h5 class="mt-6">{{ nav.title }}</h5>
              <div class="container">
                <div class="row">
                  <div class="col-12 sidebar-col">
                    <nav>
                      <ul class="sidenav-list">
                        {{ nav_list(nav.links, 'sidenav-sub_list') }}
                      </ul>
                    </nav>
                  </div>
                </div>
              </div>
              {% endfor %}
            </div>
      </div>
</div>
